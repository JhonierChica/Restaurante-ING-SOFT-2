{
  "name": "restaurant-app",
  "private": true,
  "scripts": {
    "backend": "cd backend && start cmd /k mvnw.cmd spring-boot:run",
    "frontend": "cd frontend && start cmd /k npm run dev",
    "dev": "npm run backend && npm run frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
